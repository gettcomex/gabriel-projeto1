<h1>Listing queue_of_books</h1>

<table>
  <tr>
    <th>Book</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @queue_of_books.each do |queue_of_book| %>
  <tr>
    <td><%= queue_of_book.book %></td>
    <td><%= queue_of_book.user %></td>
    <td><%= link_to 'Show', queue_of_book %></td>
    <td><%= link_to 'Edit', edit_queue_of_book_path(queue_of_book) %></td>
    <td><%= link_to 'Destroy', queue_of_book, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Queue of book', new_queue_of_book_path %>
