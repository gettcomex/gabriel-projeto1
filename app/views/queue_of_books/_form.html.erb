<%= form_for(@queue_of_book) do |f| %>
  <% if @queue_of_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@queue_of_book.errors.count, "error") %> prohibited this queue_of_book from being saved:</h2>

      <ul>
      <% @queue_of_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book %><br />
    <%= f.text_field :book %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
