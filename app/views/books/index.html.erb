<h1>Listing books</h1>

<%= link_to 'New', new_book_path %>

<table>
	<tr>
		<th>Título</th>
		<th>Escritor</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>

<% @books.each do |book| %>
	<tr>
		<td><%= book.title %></td>
		<td><%= book.writer %></td>
		<td><%= link_to 'Edit', edit_book_path(book) %></td>
	</tr>
<% end %>
</table>

<br />

<%= paginate @books %>
